# Results

Running the notebooks writes detailed predictions and evaluation metrics here.
The files are excluded from Git by default because model outputs can be large.
`metrics_summary.json` records the validated portfolio metrics.
